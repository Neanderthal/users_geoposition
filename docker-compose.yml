services:
  geoposition:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./campaigns.yaml:/app/campaigns.yaml:ro
    environment:
      - BASE_URL=http://localhost:8000
      - API_KEY=changeme
    restart: unless-stopped
