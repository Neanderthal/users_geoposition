# Copy to vars.yml and fill in your values
domain: geo.yourdomain.com
email: you@example.com           # for Let's Encrypt
api_key: changeme                # API key for download endpoint
app_dir: /opt/geoposition
